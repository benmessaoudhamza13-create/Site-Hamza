import type { Metadata } from "next";

export const metadata: Metadata = { title: "Projets — Hamza Benmessaoud" };

export default function ProjetsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
