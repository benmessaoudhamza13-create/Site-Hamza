import type { Metadata } from "next";

export const metadata: Metadata = { title: "Projets — Hamza Ben Messaoud" };

export default function ProjetsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
